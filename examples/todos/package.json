{
  "name": "todos",
  "version": "0.0.0",
  "private": true,
  "description": "The proof-of-concept application deployed by the demo script: an HTTP API on Lambda reading todos from RDS Postgres",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
